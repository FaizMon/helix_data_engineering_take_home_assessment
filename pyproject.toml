[project]
name = "helix-data-pipeline"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "apache-airflow>=2.9",
    "duckdb>=1.0",
    "structlog>=24.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
